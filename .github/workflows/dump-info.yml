name: dump-info

on:
  push:

jobs:
  some-job:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v3
      - name: Context info dump
        run: |
          echo github.job.job_id: ${{ github.job.job_id }}
          echo github.job_id: ${{ github.job_id }}
        shell: bash
